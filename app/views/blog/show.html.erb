<!DOCTYPE html>
<html lang="ja" data-theme="light" x-data="{ darkMode: localStorage.getItem('pico-theme') === 'dark' }">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= @post[:title] %> - Staticle Blog</title>

    <!-- スタイルシート -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@latest/css/pico.min.css">
    <%= stylesheet_link_tag "style.css" %>

    <!-- フォント -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:ital,opsz,wght@0,14..32,100..900;1,14..32,100..900&family=Lora:ital,wght@0,400..700;1,400..700&family=Noto+Sans+JP:wght@100..900&display=swap" rel="stylesheet">

    <!-- HTMX & Alpine.js -->
    <script src="https://unpkg.com/htmx.org@1.9.10" defer></script>
    <script src="https://unpkg.com/alpinejs@3.13.5/dist/cdn.min.js" defer></script>
</head>
<body>
    <main class="container">
        <!-- ナビゲーション -->
        <nav>
            <ul class="logo-container">
                <li><h1 class="logo"><%= link_to "Staticle", root_path, style: "text-decoration: none;" %></h1></li>
            </ul>
            <!-- ハンバーガーメニュー -->
            <div class="hamburger-menu">
                <span class="menu-text">menu</span>
                <span class="close-text">close</span>
            </div>
            <!-- メニュー -->
            <ul class="nav-menu">
                <li style="--i:1;"><%= link_to "About me", "#{root_path}#about-me" %></li>
                <li style="--i:2;"><%= link_to "Blog", blog_path, class: "active-link" %></li>
                <li style="--i:3;"><button id="theme-toggle-button" class="contrast" x-on:click="darkMode = !darkMode">Theme</button></li>
            </ul>
        </nav>

        <!-- 記事コンテンツ -->
        <article class="blog-post fade-in">
            <header>
                <%= link_to "← 記事一覧へ戻る", blog_path, class: "back-link fade-in delay-1" %>
                <h2 class="fade-in"><%= @post[:title] %></h2>
                <div class="post-meta fade-in delay-1">
                    <time><%= @post[:formatted_date] %></time>
                    <% if @post[:category].present? %>
                        <%= link_to @post[:category], blog_category_path(@post[:category]), class: "tag #{@post[:category]}" %>
                    <% end %>
                </div>
                <div class="post-tags fade-in delay-2">
                    <% @post[:tags]&.each do |tag| %>
                        <%= link_to tag, blog_tag_path(tag), class: "tag" %>
                    <% end %>
                </div>
            </header>

            <div class="post-content fade-in delay-3">
                <%= raw @post[:content] %>
            </div>

            <!-- 関連記事 -->
            <div class="related-posts fade-in delay-4">
                <h3>関連記事</h3>
                <div class="related-grid">
                    <% if @related_posts.any? %>
                        <% @related_posts.each do |related| %>
                            <article class="related-card">
                                <%= link_to blog_post_path(related[:slug]), style: "text-decoration: none; color: inherit;" do %>
                                    <h4><%= related[:title] %></h4>
                                    <div class="related-meta">
                                        <time><%= related[:formatted_date] %></time>
                                        <% related[:tags]&.first(2)&.each do |tag| %>
                                            <span class="tag"><%= tag %></span>
                                        <% end %>
                                    </div>
                                    <% if related[:excerpt].present? %>
                                        <p class="card-excerpt"><%= related[:excerpt] %></p>
                                    <% end %>
                                <% end %>
                            </article>
                        <% end %>
                    <% else %>
                        <p>関連記事はありません</p>
                    <% end %>
                </div>
            </div>
        </article>
    </main>

    <footer class="container">
        <hr>
        <p><small>&copy; 2025 Hiroto Furugen</small></p>
    </footer>

    <!-- スクリプト -->
    <%= javascript_importmap_tags %>
</body>
</html>
